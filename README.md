# EscapeRoom
Escape Room Cover Letter

export SIMULATION_MODE=true
python run.py

Visit http://localhost:5000

To simulate inputs in Python REPL:

from controller.main import inputs
inputs.trigger("button_a")
inputs.trigger("switch_b")
